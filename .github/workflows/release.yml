name: Release

on:
  push:
    tags:
      - '*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Extract version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Run frontend typecheck
        working-directory: ./frontend
        run: |
          npm install
          npm run typecheck

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push opm to Docker Hub
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            docker.io/styliteag/opm:${{ steps.version.outputs.version }}
            docker.io/styliteag/opm:latest
          provenance: false
          sbom: false
          cache-from: type=registry,ref=docker.io/styliteag/opm:buildcache
          cache-to: type=registry,ref=docker.io/styliteag/opm:buildcache,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.version }}

      - name: Build and push opm to GHCR
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/styliteag/opm:${{ steps.version.outputs.version }}
            ghcr.io/styliteag/opm:latest
          provenance: false
          sbom: false
          cache-from: type=registry,ref=ghcr.io/styliteag/opm:buildcache
          cache-to: type=registry,ref=ghcr.io/styliteag/opm:buildcache,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.version }}

      - name: Build and push opm-scanner to Docker Hub
        uses: docker/build-push-action@v6
        with:
          context: ./scanner
          file: ./scanner/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            docker.io/styliteag/opm-scanner:${{ steps.version.outputs.version }}
            docker.io/styliteag/opm-scanner:latest
          provenance: false
          sbom: false
          cache-from: type=registry,ref=docker.io/styliteag/opm-scanner:buildcache
          cache-to: type=registry,ref=docker.io/styliteag/opm-scanner:buildcache,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.version }}

      - name: Build and push opm-scanner to GHCR
        uses: docker/build-push-action@v6
        with:
          context: ./scanner
          file: ./scanner/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/styliteag/opm-scanner:${{ steps.version.outputs.version }}
            ghcr.io/styliteag/opm-scanner:latest
          provenance: false
          sbom: false
          cache-from: type=registry,ref=ghcr.io/styliteag/opm-scanner:buildcache
          cache-to: type=registry,ref=ghcr.io/styliteag/opm-scanner:buildcache,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.version }}

      - name: Build and push opm-scanner-gvm to Docker Hub
        uses: docker/build-push-action@v6
        with:
          context: ./scanner
          file: ./scanner/Dockerfile.gvm
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            docker.io/styliteag/opm-scanner-gvm:${{ steps.version.outputs.version }}
            docker.io/styliteag/opm-scanner-gvm:latest
          provenance: false
          sbom: false
          cache-from: type=registry,ref=docker.io/styliteag/opm-scanner-gvm:buildcache
          cache-to: type=registry,ref=docker.io/styliteag/opm-scanner-gvm:buildcache,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.version }}

      - name: Build and push opm-scanner-gvm to GHCR
        uses: docker/build-push-action@v6
        with:
          context: ./scanner
          file: ./scanner/Dockerfile.gvm
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/styliteag/opm-scanner-gvm:${{ steps.version.outputs.version }}
            ghcr.io/styliteag/opm-scanner-gvm:latest
          provenance: false
          sbom: false
          cache-from: type=registry,ref=ghcr.io/styliteag/opm-scanner-gvm:buildcache
          cache-to: type=registry,ref=ghcr.io/styliteag/opm-scanner-gvm:buildcache,mode=max
          build-args: |
            VERSION=${{ steps.version.outputs.version }}

      - name: Delete old GHCR package versions (opm)
        uses: actions/delete-package-versions@v5
        continue-on-error: true
        with:
          package-name: "opm"
          package-type: "container"
          min-versions-to-keep: 5
          delete-only-untagged-versions: false

      - name: Delete old GHCR package versions (opm-scanner)
        uses: actions/delete-package-versions@v5
        continue-on-error: true
        with:
          package-name: "opm-scanner"
          package-type: "container"
          min-versions-to-keep: 5
          delete-only-untagged-versions: false

      - name: Delete old GHCR package versions (opm-scanner-gvm)
        uses: actions/delete-package-versions@v5
        continue-on-error: true
        with:
          package-name: "opm-scanner-gvm"
          package-type: "container"
          min-versions-to-keep: 5
          delete-only-untagged-versions: false

      - name: Extract release notes from CHANGELOG
        id: release_notes
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          python3 << EOF > /tmp/release_notes.md
          import re
          
          version = "${{ steps.version.outputs.version }}"
          
          with open('CHANGELOG.md', 'r') as f:
              content = f.read()
          
          # Pattern to match the version section: ## [VERSION] - DATE followed by content until next ##
          # Escape dots in version for regex
          version_escaped = re.escape(version)
          pattern = rf'^## \[{version_escaped}\][^\n]*\n(.*?)(?=^## \[|\Z)'
          
          match = re.search(pattern, content, re.MULTILINE | re.DOTALL)
          
          if match:
              # Get the full matched section
              notes = match.group(0).strip()
              # Remove the header line (## [VERSION] - DATE)
              notes = re.sub(rf'^## \[{version_escaped}\][^\n]*\n', '', notes).strip()
              if notes:
                  print(notes)
              else:
                  print(f"Release {version}")
          else:
              print(f"Release {version}")
          EOF
          
          # Set output using multiline syntax
          echo "notes<<RELEASE_NOTES_EOF" >> $GITHUB_OUTPUT
          cat /tmp/release_notes.md >> $GITHUB_OUTPUT
          echo "RELEASE_NOTES_EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: Release ${{ steps.version.outputs.version }}
          body: ${{ steps.release_notes.outputs.notes }}
          generate_release_notes: false
